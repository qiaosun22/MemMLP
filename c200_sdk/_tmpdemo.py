import ctypes
from pathlib import Path
import os

# FILE_PATH = os.path.path()


# print(FILE_PATH)
# par_dir = Path(FILE_PATH).parent
# print(par_dir)
# dir = os.path.abspath(os.curdir)
# print(os.path.abspath(os.curdir))
# CLIB_PATH = f'./libBaseApi.so'
# ctypes.cdll.LoadLibrary(CLIB_PATH)
